name: Supabase Keep Alive (chesmi-training)

on:
  schedule:
    - cron: "0 */6 * * *"   # Runs every 6 hours
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase (chesmi-training)
        run: |
          echo "Pinging Supabase keep-alive endpoint..."
          curl -I https://xbjatqnptzdmxsikvhsq.supabase.co/
